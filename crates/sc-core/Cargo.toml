[package]
name = "sc-core"
version = "0.1.0"
edition = "2021"
description = "Shared types and IPC client for SmartCool fan control"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ron = "0.8"
anyhow = "1"
