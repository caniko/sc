[package]
name = "sc-detect"
version.workspace = true
edition.workspace = true
publish = false
description = "Hardware discovery for SmartCool — hwmon chip enumeration, sensor/fan detection"

[dependencies]
anyhow.workspace = true
